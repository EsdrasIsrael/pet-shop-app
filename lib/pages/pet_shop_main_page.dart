import 'package:flutter/material.dart';

class PetShopMainPage extends StatelessWidget {
  const PetShopMainPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pet Shop'),
        ),
      body: Text("data"),
    );
  }
}